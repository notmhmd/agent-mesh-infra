# Copy to agent-mesh-infra/.env on the server. Do not commit filled files.
# Use strong unique passwords; rotate any key ever pasted in chat.

POSTGRES_USER=agent
POSTGRES_PASSWORD=CHANGEME_STRONG_DB_PASSWORD
POSTGRES_DB=agent_mesh

APCA_API_KEY_ID=
APCA_API_SECRET_KEY=
# If your execution layer reads a base URL for Alpaca paper:
# APCA_API_BASE_URL=https://paper-api.alpaca.markets

LLM_MODEL=gemini/gemini-2.0-flash
GEMINI_API_KEY=

# Optional aliases (LiteLLM may use either for Google)
# GOOGLE_API_KEY=

CADDY_DOMAIN=mesh.skrr.cloud

# --- Hermes (deploy/hermes/gateway-data/.env) — not all belong in root .env ---
# TELEGRAM_BOT_TOKEN=
# TELEGRAM_ALLOWED_USERS=601954909
# Plus LLM keys per Hermes docs

# --- Optional app keys (if you wire them in custom code) ---
# NEWS_API_KEY=
